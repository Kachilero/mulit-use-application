/**
 * Theme Switcher
 *
 * This will take the 'isLight' Prop from the store and return a style
 * object that can be used by React
 */

// Typescript declarations

// Class that returns the proper styles
class ThemeSwitcher {}

/*
  // Since these are "string literals" we need to cast them
  _userThemeOverrides = themeProp => {
    if (themeProp) {
      return {
        bg: 'light',
        variant: 'light' as 'light',
        fixed: 'top' as 'top'
      };
    }
    return {
      bg: 'dark',
      variant: 'dark' as 'dark',
      fixed: 'top' as 'top'
    };
  };
*/
